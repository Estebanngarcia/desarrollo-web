//los arrays nos permiten generar una coleccion de datos ordenados

/*usamos corchetes("[]") para indicar el inicio y fin de un array. y separamos con coma (",") a sus elementos.

dentro de un array podemos almacenar la cantidad de elementos que queramos sin importar el tipo de dato de cada uno. 

en un mismo array podemos tener datos de tipo string, number, boolean y todos los demas.*/

let miArray = ["edad", 19, true, ];

/*cada dato de un array ocupa una posicion numerada conocida como índice. la primera posicion de un array es siempre 0 
 "edad" = posicion 0,
  19 = posicion 1,
  true = posicion 2;
*/

//para conocer la longitud del array, nombramos al array seguido de un punto, escribiremos la palabra length.

//ej = myArray.length; devuelve como resultado 3